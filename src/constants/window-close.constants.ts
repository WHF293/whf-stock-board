/**
 * 窗口关闭行为常量（Tauri 桌面端：标题栏 × / Alt+F4 的关闭语义）
 */

/**
 * 点击关闭按钮是否最小化到托盘
 *
 * 默认 `true`：点击标题栏 ×（或 Alt+F4）仅隐藏到系统托盘，托盘菜单「退出」才真正关闭；
 * 置为 `false` 恢复传统行为（关闭即退出）。浏览器开发模式无窗口概念，此配置不生效。
 */
export const CLOSE_TO_TRAY_DEFAULT = true;
