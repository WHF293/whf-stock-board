/**
 * 股票账户常量
 */

/** 默认账户 id（固定值，不可删除） */
export const DEFAULT_ACCOUNT_ID = 'default';

/** 默认账户名称 */
export const DEFAULT_ACCOUNT_NAME = '默认账户';

/** 交割单导入接受的文件扩展名（input accept 属性） */
export const TRADE_IMPORT_ACCEPT = '.csv,.xlsx,.xls';

/** 交割单导入文件大小上限（字节，10MB） */
export const TRADE_IMPORT_MAX_SIZE = 10 * 1024 * 1024;
