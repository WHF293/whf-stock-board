/**
 * 左侧导航栏常量
 *
 * 桌面端支持收起（仅显示图标 + hover tooltip）；
 * 窄屏为抽屉，不受收起状态影响
 */

/** 侧栏收起状态默认值（false = 默认展开） */
export const SIDEBAR_COLLAPSED_DEFAULT = false;

/** 侧栏展开宽度（像素，对应 Tailwind w-56） */
export const SIDEBAR_WIDTH_EXPANDED_PX = 224;

/** 侧栏收起宽度（像素，对应 Tailwind w-16） */
export const SIDEBAR_WIDTH_COLLAPSED_PX = 64;
